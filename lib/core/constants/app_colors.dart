import 'package:flutter/material.dart';

class AppColors {
  // Cor primária: Azul Safira Intenso
  static const Color primary = Color(0xFF1A56DB);

  // Cor secundária: Verde-Água Sereno
  static const Color secondary = Color(0xFF0E9F6E);

  // Cores de suporte (opcional, mas recomendado para um ColorScheme completo)
  static const Color background = Color(0xFFF9FAFB);
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFE02424);
  static const Color onPrimary = Colors.white;
  static const Color onSecondary = Colors.white;
}
